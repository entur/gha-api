name: Test lint
on:
  push:
jobs:
  upload-artifact:
    steps:
      - uses: actions/checkout@v4
      - name: Save spec to the artifact storage
        uses: actions/upload-artifact@v4
        with:
          name: organisation-register-backend_organisations-v3.json
          path: organisation-register-backend_organisations-v3.json
          retention-days: 1
    runs-on: ubuntu-24.04
  
  lint:
    needs: upload-artifact
    uses: ./.github/workflows/lint.yml
    with:
      artifact_glob: organisation-register-backend_organisations-v3.json
      #file_glob: order.json
      upload_to_bucket: true